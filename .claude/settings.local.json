{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m 'perf\\(upload\\): 2x concurrency, drop sign stagger, switch to worker pool' -m 'S3 handles much higher concurrency than Supabase. Tunings that were defensive against Supabase rate limits actively slowed S3 uploads down. *)",
      "Bash(git push *)"
    ]
  }
}
